<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>cdm-parent</artifactId>
        <groupId>org.finos.cdm</groupId>
        <version>0.0.0.master-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>${cdm.artifactId}</artifactId>
    <name>rosetta-source</name>

    <properties>
        <maven.compiler.release>8</maven.compiler.release>
        <!--
          Property-driven artifactId lets profiles like -P fpml / -P fixml
          publish a differently-named jar without hitting Maven's
          module-can't-depend-on-itself rule. Default value keeps the
          module artifact as cdm-java for the standard build path.
        -->
        <cdm.artifactId>cdm-java</cdm.artifactId>
    </properties>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <configuration>
                            <sourcepath>
                                ${project.build.sourceDirectory}:${project.basedir}/src/generated/java
                            </sourcepath>
                            <notimestamp>true</notimestamp>
                            <quiet>true</quiet>
                            <additionalJOption>${maven-javadoc-plugin.option}</additionalJOption>
                            <maxmemory>2g</maxmemory>
                            <excludePackageNames>fpml.*:cdm.ingest.*:cdm.mapping.*</excludePackageNames>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>format</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.finos.rune</groupId>
                        <artifactId>rune-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>format-rosetta</id>
                                <phase>validate</phase>
                                <goals>
                                    <goal>format</goal>
                                </goals>
                                <configuration>
                                    <path>${project.basedir}/src/main/rosetta</path>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>full</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-release-notes</id>
                                <phase>validate</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/classes/release</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${basedir}</directory>
                                            <includes>
                                                <include>RELEASE.md</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <artifactId>exec-maven-plugin</artifactId>
                        <groupId>org.codehaus.mojo</groupId>
                        <executions>
                            <execution>
                                <id>Check code snippets vs cdm model</id>
                                <phase>test</phase>
                                <goals>
                                    <goal>java</goal>
                                </goals>
                                <configuration>
                                    <classpathScope>test</classpathScope>
                                    <mainClass>org.finos.cdm.documentation.DocumentationCodeValidator</mainClass>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <descriptors>
                                <descriptor>assembly.xml</descriptor>
                            </descriptors>
                            <appendAssemblyId>true</appendAssemblyId>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-main-sources</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>typescript</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.finos.rune</groupId>
                        <artifactId>rune-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>generate-typescript-src</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                                <configuration>
                                    <rosettaConfig>${project.basedir}/src/main/resources/rosetta-config.yml</rosettaConfig>
                                    <sourceRoots>
                                        <sourceRoot>${project.build.directory}/classes/cdm/rosetta</sourceRoot>
                                    </sourceRoots>
                                    <!-- Matches the Rosetta lib jar file from the class path - this is used to get the basictype and annotations.rosetta files -->
                                    <classPathLookupFilter>.*org[\\/]finos[\\/]rune[\\/]rune-runtime.*\.jar</classPathLookupFilter>
                                    <languages>
                                        <language>
                                            <setup>org.isda.cdm.generators.CDMRosettaSetup</setup>
                                            <outputConfigurations>
                                                <!-- TypeScript output folder -->
                                                <outputConfiguration>
                                                    <name>SRC_GEN_TYPESCRIPT_OUTPUT</name>
                                                    <outputDirectory>src/generated/typescript</outputDirectory>
                                                </outputConfiguration>
                                            </outputConfigurations>
                                        </language>
                                    </languages>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-src-generated-typescript</id>
                                <phase>process-resources</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/classes/cdm/typescript</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${basedir}/src/generated/typescript</directory>
                                            <includes>
                                                <include>**/*.ts</include>
                                            </includes>
                                            <filtering>true</filtering>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>excel</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.finos.rune</groupId>
                        <artifactId>rune-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>generate-excel-src</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                                <configuration>
                                    <rosettaConfig>${project.basedir}/src/main/resources/rosetta-config.yml</rosettaConfig>
                                    <sourceRoots>
                                        <sourceRoot>${project.build.directory}/classes/cdm/rosetta</sourceRoot>
                                    </sourceRoots>
                                    <!-- Matches the Rosetta lib jar file from the class path - this is used to get the basictype and annotations.rosetta files -->
                                    <classPathLookupFilter>.*org[\\/]finos[\\/]rune[\\/]rune-runtime.*\.jar</classPathLookupFilter>
                                    <languages>
                                        <language>
                                            <setup>org.isda.cdm.generators.CDMRosettaSetup</setup>
                                            <outputConfigurations>
                                                <!-- Excel output folder -->
                                                <outputConfiguration>
                                                    <name>SRC_GEN_EXCEL_OUTPUT</name>
                                                    <outputDirectory>src/generated/excel</outputDirectory>
                                                </outputConfiguration>
                                            </outputConfigurations>
                                        </language>
                                    </languages>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!--
              Core-only build: excludes FpML ingest .rosetta files, the FpML upstream
              rosetta dependency-unpack, and the src/ingest-fpml/java/ Java source root.
              Produces a cdm-java artifact without any cdm.ingest.fpml.* classes.
              Consumes the reflective install in CdmRuntimeModule (no static coupling).
            -->
            <id>core</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-rune-fpml-rosetta</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-cli</id>
                                <phase>initialize</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${basedir}/target/classes/cdm/rosetta</outputDirectory>
                                    <resources combine.self="override">
                                        <resource>
                                            <directory>src/main/rosetta</directory>
                                            <includes>
                                                <include>*.rosetta</include>
                                            </includes>
                                            <excludes>
                                                <exclude>ingest-fpml-*.rosetta</exclude>
                                                <exclude>ingest-fixml-*.rosetta</exclude>
                                            </excludes>
                                            <filtering>true</filtering>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>add-ingest-fpml-source</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!--
              Codelists-only build: produces a jar containing ONLY the
              pre-generated codelist json data (229 files under
              codelist/json/). Skips all Rosetta code generation, Java
              compilation, FpML unpack, .rosetta resource copy, code
              snippet validation, and the XML→JSON transformer (data
              is already checked in to src/main/resources/codelist/json/).

              Use case: publishing codelist data refreshes without
              rebuilding the model or cutting a full cdm-java release.
            -->
            <id>codelists</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-rune-fpml-rosetta</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-cli</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.finos.rune</groupId>
                        <artifactId>rune-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-cli</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>add-ingest-fpml-source</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>add-test-source</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>Check code snippets vs cdm model</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>transform-codelist-xml-to-json</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <skipMain>true</skipMain>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-checkstyle-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <configuration>
                            <includes>
                                <include>codelist/**</include>
                                <include>META-INF/**</include>
                            </includes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!--
              FpML mappings-only build. Produces a cdm-java-fpml jar
              containing only the FpML ingest surface (compiled
              src/ingest-fpml/java + rune-generated FpML types +
              metafields wrappers). Core Rosetta types resolve from a
              pre-installed cdm-java jar on the classpath.

              Requires a prior `mvn -pl rosetta-source install -P core`
              so that org.finos.cdm:cdm-java is resolvable from ~/.m2.

              In-module analogue of the sibling rosetta-source-fpml/
              module. Uses ${cdm.artifactId} indirection so the
              profile's cdm-java dependency doesn't create a
              module-depends-on-itself cycle.

              Usage:
                mvn -pl rosetta-source install -P core -DskipTests
                mvn -pl rosetta-source install -P fpml -DskipTests
            -->
            <id>fpml</id>
            <properties>
                <cdm.artifactId>cdm-java-fpml</cdm.artifactId>
            </properties>
            <dependencies>
                <dependency>
                    <groupId>org.finos.cdm</groupId>
                    <artifactId>cdm-java</artifactId>
                    <version>${project.version}</version>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-cli</id>
                                <phase>initialize</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${basedir}/target/classes/cdm/rosetta</outputDirectory>
                                    <resources combine.self="override">
                                        <resource>
                                            <directory>${project.build.directory}/parent-dependency/fpml/rosetta</directory>
                                            <includes>
                                                <include>*.rosetta</include>
                                            </includes>
                                            <filtering>true</filtering>
                                        </resource>
                                        <resource>
                                            <directory>src/main/rosetta</directory>
                                            <includes>
                                                <include>ingest-fpml-*.rosetta</include>
                                            </includes>
                                            <filtering>true</filtering>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.finos.rune</groupId>
                        <artifactId>rune-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-cli</id>
                                <configuration>
                                    <!-- Widen classpath scan to also pick up core Rosetta types from the pinned cdm-java jar. -->
                                    <classPathLookupFilter>.*org[\\/]finos[\\/]rune[\\/]rune-runtime.*\.jar|.*org[\\/]finos[\\/]cdm[\\/]cdm-java.*\.jar</classPathLookupFilter>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-checkstyle-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>Check code snippets vs cdm model</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>transform-codelist-xml-to-json</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <configuration>
                            <!--
                              Package only the FpML surface, plus the
                              regenerated metafields wrappers (see
                              #4855 finding on rune-DSL metafield
                              duplication). Compilation runs against
                              src/main/java too but those classes are
                              filtered out at packaging.
                            -->
                            <includes>
                                <include>cdm/ingest/fpml/**</include>
                                <include>cdm/metafields/**</include>
                                <include>META-INF/**</include>
                            </includes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!--
              FIXML mappings-only build. Parallel of -P fpml for the
              FIXML surface (currently a 5-line placeholder in
              ingest-fixml-example.rosetta). In-module analogue of
              rosetta-source-fixml/. When real FIXML mappings are
              contributed, they flow through this profile unchanged.

              Requires a prior `mvn -pl rosetta-source install -P core`
              so cdm-java is resolvable from ~/.m2.

              Usage:
                mvn -pl rosetta-source install -P core -DskipTests
                mvn -pl rosetta-source install -P fixml -DskipTests
            -->
            <id>fixml</id>
            <properties>
                <cdm.artifactId>cdm-java-fixml</cdm.artifactId>
            </properties>
            <dependencies>
                <dependency>
                    <groupId>org.finos.cdm</groupId>
                    <artifactId>cdm-java</artifactId>
                    <version>${project.version}</version>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <!-- FIXML doesn't need the FpML upstream unpack. -->
                                <id>copy-rune-fpml-rosetta</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <!-- Don't compile src/ingest-fpml/java in a FIXML build. -->
                                <id>add-ingest-fpml-source</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-cli</id>
                                <phase>initialize</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${basedir}/target/classes/cdm/rosetta</outputDirectory>
                                    <resources combine.self="override">
                                        <resource>
                                            <directory>src/main/rosetta</directory>
                                            <includes>
                                                <include>ingest-fixml-*.rosetta</include>
                                            </includes>
                                            <filtering>true</filtering>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.finos.rune</groupId>
                        <artifactId>rune-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-cli</id>
                                <configuration>
                                    <classPathLookupFilter>.*org[\\/]finos[\\/]rune[\\/]rune-runtime.*\.jar|.*org[\\/]finos[\\/]cdm[\\/]cdm-java.*\.jar</classPathLookupFilter>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-checkstyle-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>Check code snippets vs cdm model</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>transform-codelist-xml-to-json</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <configuration>
                            <includes>
                                <include>cdm/ingest/fixml/**</include>
                                <include>cdm/metafields/**</include>
                                <include>META-INF/**</include>
                            </includes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>json-schema</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.finos.rune</groupId>
                        <artifactId>rune-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>generate-json-schema-src</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                                <configuration>
                                    <rosettaConfig>${project.basedir}/src/main/resources/rosetta-config.yml</rosettaConfig>
                                    <sourceRoots>
                                        <sourceRoot>${project.build.directory}/classes/cdm/rosetta</sourceRoot>
                                    </sourceRoots>
                                    <!-- Matches the Rosetta lib jar file from the class path - this is used to get the basictype and annotations.rosetta files -->
                                    <classPathLookupFilter>.*org[\\/]finos[\\/]rune[\\/]rune-runtime.*\.jar</classPathLookupFilter>
                                    <incrementalXtextBuild>false</incrementalXtextBuild>
                                    <languages>
                                        <language>
                                            <setup>org.isda.cdm.generators.CDMRosettaSetup</setup>
                                            <outputConfigurations>
                                                <!-- json-schema output folder -->
                                                <outputConfiguration>
                                                    <name>SRC_GEN_JSONSCHEMA_OUTPUT</name>
                                                    <outputDirectory>src/generated/jsonschema</outputDirectory>
                                                </outputConfiguration>
                                            </outputConfigurations>
                                        </language>
                                    </languages>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <!-- CodeList resource lifecycle - Ensure only CodeList JSON files are copied to target/classes/ -->
        <resources>
            <resource>
                <directory>${basedir}/src/main/resources</directory>
                <excludes>
                    <exclude>codelist/xml/**</exclude>
                    <!--<exclude>codelist/*.xsl</exclude>-->
                </excludes>
                <filtering>false</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                        <configuration>
                            <filesets>
                                <fileset>
                                    <directory>src/generated</directory>
                                    <includes>
                                        <include>**/**</include>
                                    </includes>
                                    <followSymlinks>false</followSymlinks>
                                </fileset>
                                <fileset>
                                    <directory>src/test/generated/java</directory>
                                    <includes>
                                        <include>**/**</include>
                                    </includes>
                                    <followSymlinks>false</followSymlinks>
                                </fileset>
                            </filesets>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <configuration>
                    <configLocation>checkstyle-for-deprecated-guice.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <sourceDirectories>
                        <sourceDirectory>src/main/java</sourceDirectory>
                        <sourceDirectory>src/test/java</sourceDirectory>
                    </sourceDirectories>
                </configuration>
                <executions>
                    <execution>
                        <id>Check style</id>
                        <!-- This needs to run /after/ xtend (test) source
                        generation is done. -->
                        <phase>process-test-sources</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>exec-maven-plugin</artifactId>
                <groupId>org.codehaus.mojo</groupId>
                <executions>
                    <execution>
                        <id>Check code snippets vs cdm model</id>
                        <phase>test</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <classpathScope>test</classpathScope>
                            <mainClass>org.finos.cdm.documentation.DocumentationCodeValidator</mainClass>
                        </configuration>
                    </execution>
                    <!-- CodeList resource lifecycle - Run Transformer before copying CodeList JSON files -->
                    <execution>
                        <id>transform-codelist-xml-to-json</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>org.finos.cdm.codelist.CodeListTransformer</mainClass>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven-dependency-plugin.version}</version>
                <executions>
                    <execution>
                        <id>copy-rune-fpml-rosetta</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/parent-dependency</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>com.regnosys.rune-fpml</groupId>
                                    <artifactId>rosetta-source</artifactId>
                                    <includes>fpml/rosetta/*.rosetta</includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <includeScope>runtime</includeScope>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-cli</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${basedir}/target/classes/cdm/rosetta</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/parent-dependency/fpml/rosetta</directory>
                                    <includes>
                                        <include>*.rosetta</include>
                                    </includes>
                                    <filtering>true</filtering>
                                </resource>
                                <resource>
                                    <directory>src/main/rosetta</directory>
                                    <includes>
                                        <include>*.rosetta</include>
                                    </includes>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <!-- CodeList resource lifecycle - Ensure only CodeList JSON files are copied to target/classes/ -->
                    <execution>
                        <id>copy-json-codelist-resources</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}/src/main/resources/codelist</directory>
                                    <includes>
                                        <include>json/*.json</include>
                                    </includes>
                                    <excludes>
                                        <exclude>xml/**</exclude>
                                        <exclude>*.xsl</exclude>
                                    </excludes>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                            <outputDirectory>${project.build.outputDirectory}/codelist</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>src/generated/java/</source>
                                <source>src/main/rosetta/</source>
                            </sources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>add-ingest-fpml-source</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>src/ingest-fpml/java/</source>
                            </sources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>add-test-source</id>
                        <phase>generate-test-sources</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>src/test/generated/java/</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.finos.rune</groupId>
                <artifactId>rune-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-cli</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <rosettaConfig>${project.basedir}/src/main/resources/rosetta-config.yml</rosettaConfig>
                            <sourceRoots>
                                <sourceRoot>${project.build.directory}/classes/cdm/rosetta</sourceRoot>
                            </sourceRoots>
                            <!-- Matches the Rosetta lib jar file from the class path - this is used to get the basictype and annotations.rosetta files -->
                            <classPathLookupFilter>.*org[\\/]finos[\\/]rune[\\/]rune-runtime.*\.jar</classPathLookupFilter>
                            <languages>
                                <language>
                                    <setup>com.regnosys.rosetta.RosettaStandaloneSetup</setup>
                                    <outputConfigurations>
                                        <!-- Default output folder -->
                                        <outputConfiguration>
                                            <outputDirectory>src/generated/java/</outputDirectory>
                                        </outputConfiguration>
                                    </outputConfigurations>
                                </language>
                            </languages>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <parameters>true</parameters>
                    <excludes>
                        <exclude>**/*.rosetta</exclude>
                    </excludes><source>8</source><target>8</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.finos.rune-common</groupId>
            <artifactId>rune-common</artifactId>
        </dependency>
        <dependency>
            <groupId>org.finos.rune</groupId>
            <artifactId>rune-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>com.regnosys.rune-fpml</groupId>
            <artifactId>rosetta-source</artifactId>
        </dependency>
        <dependency>
            <groupId>org.finos.rune-testing</groupId>
            <artifactId>rune-testing</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.opengamma.strata</groupId>
            <artifactId>strata-basics</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- org.finos.CodeListTransformer dependency -->
        <dependency>
            <groupId>net.sf.saxon</groupId>
            <artifactId>Saxon-HE</artifactId>
        </dependency>
    </dependencies>
</project>